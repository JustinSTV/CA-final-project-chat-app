import styled from "styled-components";

const StyledSection = styled.section`
  
`;

const LoginPage = () => {
  return (
    <StyledSection>
      Login page
    </StyledSection>
  );
}
 
export default LoginPage;